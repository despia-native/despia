
## W18 - THE PARITY ASCENT (2026-08-20, the owner's iOS-grade ruling)

The ruling: iOS is the reference; Android structure-right/details-wrong; web at or below
Android; ALL renderers reach iOS grade, and when one rises all rise. Executed device-free,
with the device half handed off.

THE CONVERGENCE HEADLINE (Compose Desktop vs the web plane, 10 fixtures x 2 widths, the
committed differ, no tolerance touched): breaches 825 -> 208; paired 600/600; box agreement
x 59.3 -> 99.0%, y 60.6 -> 86.3%, w 56.4 -> 94.7%, h 77.3 -> 91.7%; all-four-axes clean
18.0% -> 78.5%; breach-free captures 0/20 -> 7/20. ALL 208 remaining rows classified with
named causes (96 derived shifts under control frames, 44 control-frame geometry, 33 control
radius, 18 split pairing artifact, 12 field/form frame composition, 4 Table frame, 1 text
wrap flip) - the control-frame classes collapse when desktop grows web-grade control frames
(named follow-up), the rest is the device diff's expected residue. Element-layer causes
fixed corpus-first: D3 (align-self + max-width in CssBridge), D6 (tabs head-as-pane, fixed
in shared :core LayoutSemantics consumed by BOTH :render and desktop), Material letter-
spacing leaking into DSX text, the align-anchor hug-centering defect (the >1100px hstack
class), divider stretch law, flex-shrink min-content floors, clamped-hug definite sizing,
sheet presented in-scene, form submit anatomy; layout corpus grew (dsxcss fixtures 15-18,
flex-semantics rows; layout-oracle 17 -> 18/18).

THE CONTROLS (the owner's named findings): Android FILLED TextField is the field default
(textfield/textarea/field; the outlined variant retired as default; FormsAccessoryTest pins
it). THE SWITCH ANIMATION ROOT CAUSE: material3 SwitchImpl takes SnapSpec when the press
interaction is still active during the store round-trip recomposition - the thumb JUMPS.
Fix: SystemSwitch one-frame checked mirror (StackSystemControls.kt) mounted by both toggle
paths; PROVEN by a clock-stepped JVM oracle (full 40px M3 travel with a strictly-between
mid frame through the real store round-trip) + a disease test that keeps the raw component's
snap visible so the mirror retires loudly if material3 fixes it. iOS FIELD WELL: DSXInputWell
(TextField.swift) - 12pt padding, 44pt floor, secondarySystemFill continuous 10pt rounded
rect, 2pt accent focus ring fading 0.15s; shared by TextField/TextArea/Field/OTP; stands
down inside the system Form; parse+balance gated, compile-pending. OTP active-box motion
fixed. system-defaults.md AMENDED (dated): library-grade defaults, animated-system-control
rule, the all-rise rule. check_renderer_constants 47 facts, 0 differ (new motion fact).

THE STUDIO CASE: the 22-vs-18 facet gap explained file-by-file (3 consolidations + JNI face;
4 true gaps are the honest AI/plugin deferrals that render the red "isn't on Android yet"
agent-card face, never a blank hole); every element/component/call StudioEditor.dsx uses has
a phone implementation (verified by census x registry grep); the desktop row-grow tiling
defect the Studio render surfaced (grow=width children filling instead of sharing a hug row)
fixed fixture-first (18-row-grow-hug). F7 FIXED on three kernels corpus-first (19 lazy-
continuation rows, 26 -> 45 markdown cases; TS 53/53, Kotlin conformance green, Swift
registered in RecordMain; docs re-scan: 675 wrapped items / 0 broken, 3 pages eyeballed).
Integrator also fixed a real :platform staging race (NetworkBackend resumed continuations
before deleting the .part file).

Gates, my own runs: differ 208, gradle green (2611 + desktop 178 per integrator's forced
rerun), dom 574/574, conformance 857/857, layout 18/18, web parity 10/10 (zero pixel delta),
swift parse 455/0, constants 0 differ, guards 17/1423, prose OK, module rules 0/0.
Scoreboard: 151, ZERO delta - correct, kotlin fixes carry no device evidence.

THE HANDOFF: /home/user/despia-framework/HANDOFF-DEVICE-AGENT.md (606 lines, 8 sections,
16 Verify lines, dash-free, every command lifted from codemagic.yaml and path-swept against
the tree) - the device agent's complete protocol: iOS unsigned-simulator compile (Xcode 26.6,
45+ pending Swift files listed by command), the record lane, the on-device walks with the
owner's acceptance list (field wells, ANIMATED switch, Studio composition), Android connected
suites + the REAL capture via android_parity_capture.sh + budget ratification + the
DSX_PARITY_NATIVE_ENFORCE=1 flip, the matrix VERIFIED protocol (device evidence only), the
landmines (per-arch vocoder hash with both values and the printer line, the 5 off-mac ruby
reds, the desktop residue classes not to chase, the never-executed post-guard CI steps), and
the compressed working law. NEXT: the device agent runs it; agent-side, the program is
complete.
