//
//  GENERATED by ClosedSource/scripts/prepare_server.rb — never hand-edit.
//
//  The data-provider barrel: every enabled provider residence, keyed by backend scheme.
//  `backendSetting` is the config key whose VALUE chooses which one boots (null = the tree
//  declares no data capability, and the bootloader installs nothing).

import { installDataProvider as p0 } from "./modules/firestore/index.ts";
import { installDataProvider as p1 } from "./modules/postgres/index.ts";

export const backendSetting: string | null = "data_backend";
export const dataProviders: { backend: string; install: (env: (key: string) => string | undefined) => Promise<{ installed: boolean; backend: string }> }[] = [
  { backend: "firestore", install: p0 },
  { backend: "postgres", install: p1 },
];
