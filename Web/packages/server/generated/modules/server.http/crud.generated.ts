//
//  GENERATED by ClosedSource/scripts/prepare_server.rb — never hand-edit.
//

// Declared CRUD for server.http — NO hand-written handler exists for these rows.
// Each export is `entity` + `op` bound to the repository: the caller's identity scopes
// the query (RLS), writes are restricted to the entity's declared fields, and id/owner_id
// are server-assigned. Regenerated by prepare_server.rb — never hand-edit.

import { crudHandler } from "../../../src/repo.ts";

export const createNote = crudHandler("note", "create");
export const deleteNote = crudHandler("note", "delete");
export const getNote = crudHandler("note", "get");
export const listNotes = crudHandler("note", "list");
export const updateNote = crudHandler("note", "update");
